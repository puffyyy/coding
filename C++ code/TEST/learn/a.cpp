#include <iostream>
#include <cstdio>
#include <cstring>
#include <cmath>
#include <cstdlib>
#include <algorithm>
#include <vector>
#include <stack>
#include <queue>
#include <set>
using namespace std;
typedef long long ll;
const int MS = 1e5 + 5;

int main()
{
    //ios::sync_with_stdio(false);
    //cin.tie(nullptr);
    double a, b, c, d;

    cin >> a >> b >> c >> d;
    cout << a + c * (sqrt(2.0) - 1) / sqrt(2.0);
    return 0;
}