#include <iostream>
#include <cstdio>
#include <cstdlib>
using namespace std;
typedef long long int ll;
/* 
如果对应位置的数码管正常工作，输出 W
对应位置的数码管永远灭，输出 0
对应位置的数码管永远亮，输出 1
对应位置的状态不能确定，输出 ?
对于非数码管的位置，输出 .
*/
int main()
{
    int n;
    scanf("%d", &n);
    for (int i = 0; i < n; ++i)
    {
        ;
    }
    return 0;
}